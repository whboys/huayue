分支：
  if  else...if else
  语法：
    if(条件1){
     //满足条件执行代码
    }
    else if(条件2){
     //满足条件执行代码
    }
    else if(条件3){
    //满足条件执行代码
    }
    else if(条件4){
    //满足条件执行代码
    }
    else{
    //其它
    }
if的嵌套
1.window.prompt("提示信息")：可以通过弹窗输入值来给变量赋值，但是输入的内容会默认成string类型，如果输入的是数字需要转化。
2.parseInt() 可以将一个字符串类型的变量转化成数字类型的变量

3.
  var a=0;
  if(){
     a=182+a;
  }

  if(){
     a=500+a;
  }
4.变量：数字字母下划线组成，数字不能开头。在以后的开发过程中，变量名要求有可读性，就是要意义的英文单词构成。
5.switch
6.三目运算

自己去完善一个业务流程？用分支





